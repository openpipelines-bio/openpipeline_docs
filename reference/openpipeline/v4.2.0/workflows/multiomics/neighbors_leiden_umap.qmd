---
package: "openpipeline"
version: "v4.2.0"
title: "neighbors_leiden_umap"
namespace: "Workflows/multiomics"
description: "Performs neighborhood search, leiden clustering and run umap on an integrated embedding."
type: "workflow"
toc: false
latest: "true"
subtitle: "openpipeline v4.2.0"
---

::: {.column-margin}

### Info

ID: `neighbors_leiden_umap`  
Namespace: `workflows/multiomics`

### Links

[{{< fa brands github >}} View source code](https://github.com/openpipelines-bio/openpipeline/tree/v4.2.0/src/workflows/multiomics/neighbors_leiden_umap)

[![](/images/viash-hub-logo.svg){.vh-icon} Browse all versions](https://www.viash-hub.com/packages/openpipeline)

[![](/images/viash-hub-logo.svg){.vh-icon} Get run instructions](https://www.viash-hub.com/packages/openpipeline/v4.2.0/components/workflows/multiomics/neighbors_leiden_umap)

:::



## Example commands

You can run the pipeline using `nextflow run`.

### View help

You can use `--help` as a parameter to get an overview of the possible parameters.

```bash
nextflow run openpipelines-bio/openpipeline \
  -r v4.2.0 -latest \
  -main-script target/nextflow/workflows/multiomics/neighbors_leiden_umap/main.nf \
  --help
```

### Run command

<details>
<summary>Example of `params.yaml`</summary>

Generate a `params.yaml` for this component on [Viash Hub](https://www.viash-hub.com/packages/openpipeline/v4.2.0/components/workflows/multiomics/neighbors_leiden_umap).

</details>

Run locally with:

```bash
nextflow run openpipelines-bio/openpipeline \
  -r v4.2.0 -latest \
  -profile docker \
  -main-script target/nextflow/workflows/multiomics/neighbors_leiden_umap/main.nf \
  -params-file params.yaml
```

:::{.callout-note}
Replace `-profile docker` with `-profile podman` or `-profile singularity` depending on the desired backend.
:::

Running on Seqera or through Viash Hub is explained in the [guides](/guides/index.qmd).

## Argument groups

### Inputs

|Name           |Description                               |Attributes                                  |
|:----------|:--------------------------------------------------|:--------------------|
|`--input`      |Path to the sample.                       |`file`, required, example: `"dataset.h5mu"` |
|`--obsm_input` |The key of the embedding to use as input. |`string`, required                          |
|`--modality`   |Which modality to process.                |`string`, default: `"rna"`                  |


### Outputs

|Name       |Description                     |Attributes                                 |
|:----------|:--------------------------------------------------|:--------------------|
|`--output` |Destination path to the output. |`file`, required, example: `"output.h5mu"` |


### Neighbour calculation

|Name                             |Description                                                                             |Attributes         |
|:----------|:--------------------------------------------------|:--------------------|
|`--uns_neighbors`                |In which .uns slot to store various neighbor output objects.                            |`string`, required |
|`--obsp_neighbor_distances`      |In which .obsp slot to store the distance matrix between the resulting neighbors.       |`string`, required |
|`--obsp_neighbor_connectivities` |In which .obsp slot to store the connectivities matrix between the resulting neighbors. |`string`, required |


### Clustering options

|Name                  |Description                                                                                                                                                                                                                                                        |Attributes                                          |
|:----------|:--------------------------------------------------|:--------------------|
|`--obs_cluster`       |Prefix for the .obs keys under which to add the cluster labels. Newly created columns in .obs will  be created from the specified value for '--obs_cluster' suffixed with an underscore and one of the resolutions resolutions specified in '--leiden_resolution'. |`string`                                            |
|`--leiden_resolution` |Control the coarseness of the clustering. Higher values lead to more clusters.                                                                                                                                                                                     |List of `double`, default: `1`, multiple_sep: `";"` |


### Umap options

|Name          |Description                                                                                               |Attributes |
|:----------|:--------------------------------------------------|:--------------------|
|`--obsm_umap` |In which .obsm slot to store the resulting UMAP embedding. When not specified, UMAP will not be executed. |`string`   |

## Authors


  * Dries Schaumont [{{< fa brands github >}}](https://github.com/DriesSchaumont) [{{< fa brands orcid >}}](https://orcid.org/0000-0002-4389-0440) [{{< fa brands linkedin >}}](https://linkedin.com/in/dries-schaumont) (author)

## Visualisation

```{mermaid}
flowchart TB
    c_cluster_leiden["leiden"]
    c_dimred_umap["umap"]
    c_metadata_move_obsm_to_obs["move_obsm_to_obs"]
    c_neighbors_find_neighbors["find_neighbors"]
    c_cluster_leiden --> c_metadata_move_obsm_to_obs
    c_metadata_move_obsm_to_obs --> c_dimred_umap
    c_neighbors_find_neighbors --> c_cluster_leiden
    style c_cluster_leiden fill:#f2f6f3,stroke:#8ba295,stroke-dasharray:5 3,color:#566b5f
    style c_dimred_umap fill:#f2f6f3,stroke:#8ba295,stroke-dasharray:5 3,color:#566b5f
    style c_metadata_move_obsm_to_obs fill:#f2f6f3,stroke:#8ba295,stroke-dasharray:5 3,color:#566b5f
    style c_neighbors_find_neighbors fill:#e2f4e7,stroke:#2b8f52,color:#13201a
```

*Dashed nodes are optional steps.*
